language: rust
cache: cargo
addons:
        apt:
                packages:
                        - kcov
os:
        - linux
        - osx
        - windows
install:
        - cargo install --force cargo-kcov
script:
        - cargo build
        - cargo test
        - cargo kcov --coveralls

